"use client";

// All content is free and open — no subscription required.
interface EntitlementGateProps {
  children: React.ReactNode;
}

export function EntitlementGate({ children }: EntitlementGateProps) {
  return <>{children}</>;
}
