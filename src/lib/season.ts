/**
 * @deprecated البرنامج لم يعد مربوطاً برمضان — يرجع دائماً false.
 * يُبقى التصدير لعدم كسر الاستيراد في الملفات الأخرى.
 */
export function isRamadanProgramClosed(): boolean {
  return false;
}
