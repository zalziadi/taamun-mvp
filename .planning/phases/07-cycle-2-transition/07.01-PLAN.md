---
phase: 07-cycle-2-transition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
autonomous: true
requirements:
  - RETURN-02
  - RETURN-05
  - NFR-08
  - NFR-09
  - NFR-10

must_haves:
  truths:
    - "New table `badges` exists in Supabase with UNIQUE(user_id, badge_code, cycle_number)"
    - "New column `progress.cycle_paused_at timestamptz NULL` exists"
    - "RLS on `badges`: authenticated user can SELECT own rows; only service role can INSERT/UPDATE/DELETE"
    - "Migration applies cleanly on a fresh schema (UP) and fully reverses (DOWN) — two-step compliant per NFR-09"
  artifacts:
    - path: "supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql"
      provides: "Additive migration — new table + new column + RLS policies"
      contains: "CREATE TABLE IF NOT EXISTS badges"
  key_links:
    - from: "badges.user_id"
      to: "auth.users.id"
      via: "foreign key constraint"
      pattern: "REFERENCES auth\\.users\\(id\\)"
    - from: "badges unique constraint"
      to: "idempotent Day-28 unlock (Plan 07.04)"
      via: "ON CONFLICT DO NOTHING relies on this"
      pattern: "UNIQUE \\(user_id, badge_code, cycle_number\\)"
---

<objective>
Deliver the additive schema changes Phase 7 requires: a new `badges` table (Day-28 will be the first consumer in Plan 07.04, days 1/3/7/14/21 + cycle-completion land in Phase 8) and a new `progress.cycle_paused_at timestamptz` column that Plan 07.02's optimistic-concurrency guard relies on.

Purpose: Unblock Plans 07.02 (concurrency guard reads the column) and 07.04 (idempotent badge insert relies on the UNIQUE constraint). Additive-only — no NOT NULL enforcement, no data backfill, no destructive changes. Phase 8 will add the remaining badge variants; this plan only creates the table shape.
Output: One new migration file that applies UP cleanly, reverses DOWN cleanly, and introduces zero runtime behavior changes on its own.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cycle-2-transition/07-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<!-- Convention reference — read the 3 most recent migrations for naming + RLS style -->
@supabase/migrations/20260418300000_book_notes.sql
@supabase/migrations/20260418200000_ai_generated_days.sql
@supabase/migrations/20260418100000_push_subscriptions.sql
@supabase/migrations/20260418000000_add_cycles_support.sql

<interfaces>
<!-- Existing `progress` table shape (do NOT redefine — only ADD the column) -->
<!-- From supabase/migrations/20260226012000_progress_table.sql + 20260418000000_add_cycles_support.sql -->
<!-- progress(user_id uuid PK, current_day int, completed_days int[], current_cycle int, completed_cycles int[], updated_at timestamptz, ...) -->

<!-- Existing RLS pattern (follow this exactly): -->
<!-- CREATE POLICY "users_select_own_{table}" ON {table} FOR SELECT USING (auth.uid() = user_id); -->
<!-- INSERT/UPDATE/DELETE restricted to service_role (bypasses RLS). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create additive migration for badges table + progress.cycle_paused_at</name>
  <files>supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql</files>
  <action>
Create ONE new migration file at the exact path above. The filename follows the existing YYYYMMDDhhmmss convention (verify against the three most recent migrations listed in &lt;context&gt;).

The file MUST contain, in order:

1. A top-of-file header comment in Arabic + English documenting:
   - Phase 7 (v1.2 Retention Loop)
   - REQs addressed: RETURN-02 (race guard column) and RETURN-05 (badges table for Day-28 silent reveal)
   - "Additive only — two-step migration per NFR-09. Enforcement (NOT NULL, extra indexes) lands in a later phase if needed."

2. ALTER statement adding `cycle_paused_at timestamptz NULL` to `progress` — with `IF NOT EXISTS` guard on the column (per Postgres 15+ `ADD COLUMN IF NOT EXISTS`). No default. Nullable. This is the race-guard column Plan 07.02 consumes.

3. CREATE TABLE IF NOT EXISTS `badges` with columns:
   - `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`
   - `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`
   - `badge_code text NOT NULL` (examples per Phase 7 scope: `day_28`; Phase 8 will add `day_1`, `day_3`, `day_7`, `day_14`, `day_21`, `cycle_complete` — do NOT seed those here)
   - `cycle_number int NOT NULL CHECK (cycle_number >= 1 AND cycle_number <= 99)`
   - `unlocked_at timestamptz NOT NULL DEFAULT now()`
   - `notified boolean NOT NULL DEFAULT false` (Phase 8 uses this for retroactive-backfill silence — see PITFALLS.md #4)
   - `UNIQUE (user_id, badge_code, cycle_number)` — load-bearing for idempotent inserts in Plan 07.04

4. Index `CREATE INDEX IF NOT EXISTS idx_badges_user_id ON badges(user_id)` for `/progress` page reads.

5. Enable RLS: `ALTER TABLE badges ENABLE ROW LEVEL SECURITY;`

6. Policies (match existing convention — see `20260401000000_pattern_insights.sql` + other recent migrations):
   - `CREATE POLICY "users_select_own_badges" ON badges FOR SELECT USING (auth.uid() = user_id);`
   - NO policy for INSERT/UPDATE/DELETE — those must go through service_role (via `getSupabaseAdmin()`), per the v1.2 architecture decision recorded in ARCHITECTURE.md §"RLS posture for new tables".

7. A DOWN-block equivalent in a trailing comment (since Supabase migrations are UP-only, document the inverse operations in a `-- DOWN:` comment so a human can copy-paste to revert in an incident): `DROP TABLE IF EXISTS badges;` and `ALTER TABLE progress DROP COLUMN IF EXISTS cycle_paused_at;`.

DO NOT:
- Seed any badge rows (backfill is Phase 8's concern).
- Add a NOT NULL constraint or default to `cycle_paused_at` (that's the "enforce" step in a future migration per NFR-09).
- Reference or alter any other table.
- Add triggers or RPCs.

Cite RETURN-02 and RETURN-05 in the header comment so executor traceability is obvious.
  </action>
  <verify>
    <automated>
# 1. Migration file exists and is non-empty
test -s supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql

# 2. Contains required structural elements (grep)
grep -q "CREATE TABLE IF NOT EXISTS badges" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
grep -q "UNIQUE (user_id, badge_code, cycle_number)" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
grep -q "cycle_paused_at" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
grep -q "users_select_own_badges" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
grep -q "REQ.*RETURN-02" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql || grep -q "RETURN-02" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
grep -q "RETURN-05" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql

# 3. No seeding, no NOT NULL on cycle_paused_at (would break additive rule)
! grep -E "INSERT INTO badges" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql
! grep -E "cycle_paused_at[[:space:]]+[a-z]+[[:space:]]+NOT NULL" supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql

# 4. Project build still compiles (no TS files changed — sanity)
npx tsc --noEmit
    </automated>
  </verify>
  <done>
- File exists at `supabase/migrations/20260419000000_v1_2_badges_and_cycle_guard.sql`.
- All grep probes above pass.
- `npx tsc --noEmit` still passes (migration is SQL only).
- Executor has NOT applied the migration to any remote database in this plan — that's a human decision per CLAUDE.md. The plan only authors the file.
  </done>
</task>

</tasks>

<verification>
Full-phase sanity on this plan:

1. Migration file present and grep-clean (see &lt;verify&gt; above).
2. `npx tsc --noEmit && npm run build` passes (no regressions introduced — this plan adds SQL only).
3. Manual review: open the file and confirm the RLS policy grants SELECT only to `auth.uid() = user_id` and contains no `FOR INSERT` / `FOR UPDATE` policy (service role must remain the only writer).
4. Open the three most recent migration files referenced in &lt;context&gt; and confirm the new file uses the same header/comment/policy naming conventions.
</verification>

<success_criteria>
- Migration file written; grep checks pass; tsc + build pass.
- `badges` table definition contains the UNIQUE constraint that Plan 07.04's idempotent unlock depends on — grep for `UNIQUE \(user_id, badge_code, cycle_number\)` returns 1 match.
- `progress.cycle_paused_at` column definition present; grep-able; nullable (no NOT NULL), no default.
- Zero behavioral change until Plans 07.02 and 07.04 consume the new schema.
</success_criteria>

<out_of_scope>
Banned anti-patterns — do NOT introduce any of the following in this plan:

- Seed data for `badges` table (retroactive backfill of day-1/3/7/14/21 belongs to Phase 8; Day-28 inserts happen at runtime via Plan 07.04, not via seed).
- Any UPDATE/INSERT/DELETE RLS policy on `badges` — writes go through service role only.
- Any schema change to `reflections`, `awareness_logs`, `profiles`, `activation_codes`, `customer_subscriptions`.
- NOT NULL / defaults on `cycle_paused_at` (that's the "enforce" step of a future two-step migration).
- Confetti, fireworks, "Unlocked!" / "Achievement" English vocabulary in comments/docs.
- Multi-step cycle-2 wizard scaffolding, streak continuation UI, "top X% of users" comparison, separate badge modal, share button on badges, auto-advance logic — none belong in a migration file anyway, listed here for invariant completeness.
- New runtime dependencies (NFR-08). This is SQL only.
- Session recording / heatmaps / autocapture hooks. Not applicable here; listed for invariant.
- Modal interrupting reflection input on Day 28. Not applicable here; listed for invariant.
</out_of_scope>

<output>
After completion, create `.planning/phases/07-cycle-2-transition/07.01-SUMMARY.md` per the standard template. Include: migration file path, grep-verification results, the exact CREATE TABLE statement used, and a note confirming RETURN-02 + RETURN-05 are now structurally unblocked for Plans 07.02 and 07.04.
</output>
