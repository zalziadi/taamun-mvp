---
phase: 07-cycle-2-transition
plan: 06
type: execute
wave: 4
depends_on: [07.02, 07.03, 07.04, 07.05]
files_modified:
  - scripts/verify/phase-07-integration.mjs
  - scripts/guards/phase-07-anti-patterns.sh
  - src/lib/calendarDay.test.ts
autonomous: false
requirements:
  - RETURN-01
  - RETURN-02
  - RETURN-03
  - RETURN-04
  - RETURN-05
  - RETURN-06
  - RETURN-07
  - NFR-02
  - NFR-06
  - NFR-10

must_haves:
  truths:
    - "Single-device happy path verified: tap CTA → current_cycle=2 → one badge row → one cycle_start + one badge_unlock event"
    - "Multi-device race verified: two concurrent start-cycle calls → 409 on the loser → exactly one cycle_start event, exactly one badge row"
    - "Timezone test verified: computeCalendarDay returns 28 for the 23:00-Riyadh-day-27 → 06:00-next-day vector"
    - "Anti-pattern grep sweep passes: zero confetti imports, zero \"unlocked!\"/\"achievement\" vocabulary, zero auto-advance, zero separate badge modal, zero share-on-badge references introduced by Phase 7"
    - "Human checkpoint signed off: Day-28 flow on real device shows inline CTA (no modal), tap transitions to Day 1 cycle 2, Day-28 badge visible on /progress without having been surfaced via modal/toast"
  artifacts:
    - path: "scripts/verify/phase-07-integration.mjs"
      provides: "Node-run integration harness covering the 3 automated ROADMAP criteria"
      contains: "cycle_race"
    - path: "scripts/guards/phase-07-anti-patterns.sh"
      provides: "Grep-based anti-pattern sweep (runs in under 5s; good for CI)"
      contains: "banned_terms"
    - path: "src/lib/calendarDay.test.ts"
      provides: "Unit test for Plan 07.05's ROADMAP #5 vector"
      contains: "Asia/Riyadh"
  key_links:
    - from: "integration harness"
      to: "Plan 07.02 /api/program/start-cycle"
      via: "mocked Supabase admin + in-process call"
      pattern: "start-cycle"
    - from: "integration harness"
      to: "Plan 07.04 unlockBadge helper"
      via: "direct import"
      pattern: "unlockBadge"
---

<objective>
Prove — with automation — that all 5 ROADMAP Phase 7 success criteria hold once Plans 07.01–07.05 have landed. Sweep the codebase for every banned anti-pattern from 07-CONTEXT.md. Capture a human checkpoint on a real device for the tonal invariants that only a person can judge (does the moment feel like a Quranic-reflection product, or did it drift toward Duolingo?).

Purpose: Close Phase 7 with confidence. Deliver verification of ALL RETURN-01..07 REQs, and ensure NFR-02 / NFR-06 / NFR-10 are respected. This plan produces the artifacts that future phases (8–11) can re-run to detect regressions.
Output: (1) An integration harness that exercises the three automatable ROADMAP criteria; (2) a CI-friendly anti-pattern grep guard; (3) a `calendarDay.test.ts` unit test for the timezone vector; (4) a human checkpoint confirming the tonal invariants on real hardware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cycle-2-transition/07-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/07-cycle-2-transition/07.01-PLAN.md
@.planning/phases/07-cycle-2-transition/07.02-PLAN.md
@.planning/phases/07-cycle-2-transition/07.03-PLAN.md
@.planning/phases/07-cycle-2-transition/07.04-PLAN.md
@.planning/phases/07-cycle-2-transition/07.05-PLAN.md
@CLAUDE.md

@src/lib/analytics/events.ts
@src/lib/analytics/server.ts
@src/lib/calendarDay.ts
@src/lib/badges/unlock.ts
@src/app/api/program/start-cycle/route.ts
@src/app/api/badges/unlock/route.ts
@src/components/DayExperience.tsx
@src/components/badges/MilestoneBadge.tsx

<interfaces>
<!-- Contracts depended on by this plan -->

From Plan 07.02 (src/app/api/program/start-cycle/route.ts):
- POST accepts `{ cycle: number, expected_current_cycle: number }`
- Returns 200 `{ ok, cycle, archived, completed_cycles }` on success; 409 `{ error: "cycle_race", current_cycle }` on race; 500 on db_error; 401 on unauth.
- Emits ONE `cycle_start` event on success only.

From Plan 07.04 (src/lib/badges/unlock.ts):
- `unlockBadge(userId, badge_code, cycle_number, day_number) → { unlocked: boolean }`
- Idempotent via UNIQUE(user_id, badge_code, cycle_number).
- Emits ONE `badge_unlock` event only when actuallyInserted.

From Plan 07.05 (src/lib/calendarDay.ts):
- `computeCalendarDay(subscriptionStartDate, now?) → number (1..28)`
- Asia/Riyadh-anchored via Intl.DateTimeFormat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit test for calendarDay Riyadh-timezone vector</name>
  <files>src/lib/calendarDay.test.ts</files>
  <action>
Create a TypeScript test file alongside `src/lib/calendarDay.ts`. If the project already uses a test runner (check `package.json` scripts and devDependencies — likely `vitest` or `jest`; if neither is installed, use the built-in `node --test` runner which is available without a new dep and satisfies NFR-08), use it. If the project has no test runner at all, write the file as a `node --test`-compatible script so `node --test src/lib/calendarDay.test.ts` (via tsx loader or after a targeted compile) can run it.

**Before editing**: the executor should verify the project's current test-runner setup via `package.json` + running `npm test 2>&1 | head` if a script exists. Do NOT install a new test runner (NFR-08).

Test vectors:

| Case | subscriptionStartDate | now | Expected |
|------|----------------------|-----|----------|
| ROADMAP #5: 23:00 Riyadh day 27 → 06:00 Riyadh day 28 | `2026-03-01T20:00:00Z` (2026-03-01 23:00 Riyadh) | `2026-03-29T03:00:00Z` (2026-03-29 06:00 Riyadh) | `28` |
| Enrollment day | `2026-03-01T10:00:00Z` | `2026-03-01T11:00:00Z` | `1` |
| Day 2 (next Riyadh calendar day) | `2026-03-01T20:00:00Z` (23:00 Riyadh day 1) | `2026-03-02T03:00:00Z` (06:00 Riyadh day 2) | `2` |
| Past day 28 (clamps) | `2026-01-01T00:00:00Z` | `2026-12-31T00:00:00Z` | `28` |
| Null input | `null` | (any) | `1` |
| Invalid date string | `"not a date"` | (any) | `1` |

Exit non-zero on any mismatch.

DO NOT:
- Install a new test runner.
- Use network / DB in this test.
- Hardcode "now" as `new Date()` — always pass an explicit `now` Date for determinism.
  </action>
  <verify>
    <automated>
test -s src/lib/calendarDay.test.ts
grep -q "Asia/Riyadh\\|computeCalendarDay" src/lib/calendarDay.test.ts
grep -q "2026-03-29T03:00:00Z" src/lib/calendarDay.test.ts

# Run the test using whatever runner the project supports.
# Adjust to the project's actual test script; example for `node --test` with tsx loader:
#   npm test -- --run src/lib/calendarDay.test.ts
# OR if using vitest:
#   npx vitest run src/lib/calendarDay.test.ts
# OR node --test after compile. Executor picks based on what is already configured.

npx tsc --noEmit
    </automated>
  </verify>
  <done>
- Test file exists, covers the 6 vectors above, executes without a new dep, all vectors pass.
- The ROADMAP #5 vector in particular returns exactly `28` — this is the load-bearing assertion for RETURN-06.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration harness for cycle-transition happy path + race</name>
  <files>scripts/verify/phase-07-integration.mjs</files>
  <action>
Create a Node ESM integration harness that exercises:

1. **Happy path (ROADMAP #1 + #2 automatable slice)**:
   - Set up: an in-memory fake "database" representing a user with `progress = { current_cycle: 1, current_day: 28, completed_days: [1..28], completed_cycles: [] }`.
   - Mock `getSupabaseAdmin()` and `createSupabaseServerClient()` + mock `fetch` to PostHog so we can COUNT event emissions without network calls. The mocks should track every emit() call site.
   - Invoke the `/api/program/start-cycle` POST handler directly (import the exported `POST` from the route file — Next.js Route Handlers are plain async functions and testable this way).
   - Assert: response status `200`, DB state shows `current_cycle=2`, `completed_cycles=[1]`, `current_day=1`, one `badges` row exists with `(user_id, "day_28", 1)`, exactly one `cycle_start` event captured, exactly one `badge_unlock` event captured.

2. **Multi-device race (ROADMAP #3 — the load-bearing test)**:
   - Same setup. Fire TWO POST handler calls concurrently via `Promise.all`.
   - Assert: one response is 200, the other is 409 `{ error: "cycle_race" }`. DB's `current_cycle` ends at `2` (not `3`). `badges` table has exactly ONE row with `(user_id, "day_28", 1)`. PostHog mock captured exactly ONE `cycle_start` event and exactly ONE `badge_unlock` event.

3. **Idempotent badge unlock (RETURN-07 badge half)**:
   - Directly invoke `unlockBadge(userId, "day_28", 1, 28)` twice in sequence.
   - Assert: first call returns `{ unlocked: true }` + one event emitted; second call returns `{ unlocked: false }` + ZERO additional events emitted.

4. **Event-not-on-race-loser**:
   - Specifically assert that on the 409 path, the PostHog mock captured zero events FROM THAT invocation (contributed by the loser). Use per-invocation event capture to distinguish.

5. **Fallback narrowing (plan-checker gap #2)**:
   - Simulate the mock Supabase throwing a non-`42703` error code (e.g., `{ code: "23505", message: "unique_violation" }`) on the update.
   - Assert: response status is `500`, NO `cycle_start` event fires, NO minimal-payload retry occurs. This guards PITFALL #2 — ensures the narrowed fallback path only activates for undefined_column errors.

**Route handler invocation pattern (plan-checker gap #4):** Route handlers expect a real `Request`. Invoke them as:
```js
const res = await POST(new Request("http://localhost/api/program/start-cycle", {
  method: "POST",
  headers: { "content-type": "application/json" },
  body: JSON.stringify({ cycle: 2, expected_current_cycle: 1 }),
}));
```
Do NOT pass plain objects — `NextResponse.json(...)` expects a Request-like input for cookies/headers context.

The harness should:
- Exit 0 when all assertions pass.
- Exit non-zero with a human-readable summary on any failure.
- Run in <10 seconds.
- Produce a brief text report printed to stdout (one line per assertion).

**Mock strategy** (critical — do this without new deps):
- For Supabase: build a tiny fake that implements `.from(table).select().eq().maybeSingle()`, `.upsert(...).select()`, `.update(...).eq().eq().select()`, and `.insert(...).select()` — enough surface for the two routes' queries. Use an in-memory Map keyed by `user_id`.
- For `fetch` to PostHog: override `globalThis.fetch` with a stub that records captures.
- For auth: the routes call `supabase.auth.getUser()` — return a fixed fake user ID.

DO NOT:
- Install new test libraries (NFR-08).
- Call the real Supabase or real PostHog.
- Use long-running timers / setTimeout-based delays.
- Leave any global fetch override in place after the harness exits.
  </action>
  <verify>
    <automated>
test -s scripts/verify/phase-07-integration.mjs

# Required assertions mentioned by name
grep -q "cycle_race" scripts/verify/phase-07-integration.mjs
grep -q "day_28" scripts/verify/phase-07-integration.mjs
grep -q "badge_unlock\\|unlockBadge" scripts/verify/phase-07-integration.mjs
grep -q "cycle_start" scripts/verify/phase-07-integration.mjs

# Harness runs clean
node scripts/verify/phase-07-integration.mjs
    </automated>
  </verify>
  <done>
- Harness exists and exits 0.
- Covers happy path, race, idempotent badge unlock, and no-event-on-race-loser.
- No new deps introduced.
- Runs in < 10s.
  </done>
</task>

<task type="auto">
  <name>Task 3: Anti-pattern grep guard</name>
  <files>scripts/guards/phase-07-anti-patterns.sh</files>
  <action>
Create a bash script that greps the repo for every banned term in the Phase 7 "Out of Scope" list and exits non-zero on any match OUTSIDE explicitly-permitted locations (e.g., planning docs, this script itself, other guard scripts).

Banned terms to sweep (subset from 07-CONTEXT.md "Banned anti-patterns" + RETURN-04 + research R4):

- `canvas-confetti` / `confetti` in source imports
- `lottie-react` / `lottie` in source imports
- Case-insensitive vocabulary in source strings: `unlocked!`, `achievement`, `level up`, `streak`, `mission accomplished`
- `<Dialog`, `<Modal`, `<Drawer`, `<Popover` components inside `src/app/day/**`, `src/components/DayExperience.tsx`, `src/components/ReflectionJournal.tsx`, `src/components/AwarenessMeter.tsx`, `src/components/VerseBlock.tsx`, `src/components/HiddenLayer.tsx`, `src/components/BookQuote.tsx`, `src/components/SilenceGate.tsx`, `src/components/badges/**`
- `share` on badge code paths: any import of `next/og` / `opengraph-image` / `og:image` from `src/components/badges/**` or `src/app/api/badges/**` is banned
- Client-side PostHog emission: `posthog.capture(` or `track(` in any file under `src/app/day/**`, `src/app/reflection/**`, `src/app/book/**`, `src/app/program/day/**`, `src/app/api/guide/**`, `src/app/guide/**`, or the 7 sacred components
- "Auto-advance" pattern heuristic: any automatic POST to `/api/program/start-cycle` from a `useEffect` with empty deps inside `DayExperience.tsx` or its descendants — the script should grep for `useEffect.*start-cycle` and fail if found

Script structure:

```bash
#!/usr/bin/env bash
set -euo pipefail

FAIL=0
report() { echo "[phase-07-guard] $1" >&2; FAIL=1; }

# Exclude planning docs + this script itself + other guard scripts
EXCLUDES='--exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=docs --exclude-dir=scripts'

# Check 1: banned libraries
if grep -rnE "from ['\"](canvas-confetti|lottie-react|lottie)['\"]" $EXCLUDES src/ 2>/dev/null; then
  report "Banned animation library imported (confetti/lottie)"
fi

# Check 2: banned English-loan vocabulary in source (allow Arabic-only files to be clean)
# ... (similar grep for each banned term, scoped to src/)

# Check 3: modal-free sacred zones
# ... (grep <Dialog|<Modal|<Drawer|<Popover inside sacred components)

# Check 4: client-side analytics on sacred paths
# ... (grep posthog.capture / track( inside sacred dirs)

# Check 5: no "auto-advance" useEffect wiring start-cycle
# ... (multiline-ish grep inside DayExperience.tsx)

if [ "$FAIL" -ne 0 ]; then
  echo "[phase-07-guard] FAIL — see reported violations above" >&2
  exit 1
fi
echo "[phase-07-guard] PASS — zero banned patterns found"
```

Make the script executable (`chmod +x`) and idempotent.

Add an NPM script entry `phase-07-guard` to `package.json` `scripts` block IF the project already uses such scripts (check `package.json`; if the existing scripts section is short and you have permission to add one script, add it; otherwise document the manual invocation in the SUMMARY).

DO NOT:
- Block on legitimate uses (e.g., research docs mentioning "confetti" as a banned pattern — that's why EXCLUDES covers `.planning/` and `docs/`).
- Use `ripgrep`-only flags (`grep` is POSIX-safe; use `-E` for extended regex).
- Fail on comments that DEFINE the banned patterns (acceptable — grep will hit them; tune EXCLUDES to cover `scripts/` so this script itself doesn't self-trigger).
  </action>
  <verify>
    <automated>
test -x scripts/guards/phase-07-anti-patterns.sh || chmod +x scripts/guards/phase-07-anti-patterns.sh
test -x scripts/guards/phase-07-anti-patterns.sh

# Script runs successfully (exits 0 because Phase 7 plans don't introduce any banned patterns)
bash scripts/guards/phase-07-anti-patterns.sh
    </automated>
  </verify>
  <done>
- Script exists, executable, exits 0 on the current codebase (proving Plans 07.01–07.05 didn't introduce banned patterns).
- Script exits non-zero if a banned pattern IS introduced in the future (regression guard).
- Reports one line per violation with file:line context.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The full Phase 7 Cycle 2 Transition + Day-28 Badge flow. Plans 07.01–07.05 have shipped the schema, the race-guarded route, the inline CTA, the silent badge unlock, and the Asia/Riyadh timezone fix. Automated checks in Tasks 1–3 of this plan verify the machine-verifiable invariants. This checkpoint covers the tonal / visual / RTL / a11y invariants that only a human can judge on real hardware.
  </what-built>
  <how-to-verify>
Test on a real device (iOS Safari AND Android Chrome if available — RTL bugs differ between them).

1. **Run the app locally** (`npm run dev`) and open on the device via the LAN URL. Sign in as a user whose `progress.current_day === 28` and `progress.completed_days` contains all 28 days. If no such user exists, use Supabase Studio to temporarily set one up; revert after the test.

2. **Day-28 view inspection**:
   - Navigate to `/program/day/28`.
   - Complete the reflection if not yet saved.
   - Confirm: the inline CTA "واصل الرحلة" appears BELOW existing elements within the same view. NO modal, dialog, drawer, or overlay opened.
   - Confirm: subtle framer-motion fade-in (under 1 second). No confetti, no fireworks, no emoji cascade.
   - Confirm: copy is Arabic-native — no English visible, no awkward machine-translation feel.
   - Confirm: RTL flow is correct — button label on the expected side, microcopy flows right-to-left, numerals render correctly.

3. **Tap the CTA** (ONCE — do not double-tap):
   - Watch: button immediately visually disables.
   - On success (2xx), the page refreshes and the user lands on Day 1 (of cycle 2) with the SAME 28 verses (Headspace model — RETURN-03). Confirm the verse content is identical to cycle 1 Day 1.

4. **Check `/progress`**:
   - Day-28 badge is visible as unlocked.
   - Zero modal / toast / notification appeared during the transition. If any did, the test FAILS.

5. **Multi-device race (if a second device is available)**:
   - Using a second authenticated session (different device or incognito window logged into the same account), navigate back to Day 28 by resetting `progress.current_day = 28` in Supabase Studio if needed (or simulate by opening the URL in both tabs before tapping).
   - Tap the CTA in both simultaneously (within a few seconds of each other).
   - Confirm: one device succeeds and shows Day 1 of cycle 2; the other shows the calm message "تم بدء الحلقة من جهاز آخر. حدّث الصفحة لمتابعة رحلتك." No crash, no loop.

6. **Timezone spot-check (if feasible)**:
   - Change device time to Asia/Riyadh 23:00 on day 27 equivalent → refresh → confirm Day 27 shows. Change to 06:00 next day → refresh → confirm Day 28 shows. Plan 07.06 Task 1 already tests this programmatically; this is a belt-and-suspenders verification.

7. **Accessibility spot-check**:
   - Lighthouse mobile run on the Day-28 view: A11y score ≥ 95 (NFR-02).
   - Tab through the CTA section with keyboard: focus order is sensible, Enter/Space triggers the button, `aria-disabled` is honored.

8. **Anti-pattern walk-through**:
   - Scroll the page: no streak counter UI, no "🔥 28 days" anywhere, no "Unlocked!" / "Achievement" copy, no share button on the badge, no countdown timer.

If ANY of the above fails, describe the failure concretely (screenshot + step number). Otherwise sign off with "approved".
  </how-to-verify>
  <resume-signal>Type "approved" after verifying all 8 steps above, or describe the concrete failure and which step.</resume-signal>
</task>

</tasks>

<verification>
All five ROADMAP Phase 7 Success Criteria, mapped:

| Criterion | Covered by |
|---|---|
| #1 Inline CTA on Day 28, no modal, no nav-away | Task 4 human verify steps 2-3 + Task 3 grep guard |
| #2 One-tap advance to Day 1 cycle 2 (same 28 verses, Headspace model) | Task 4 human verify step 3 + Task 2 integration harness happy path |
| #3 Multi-device race → current_cycle=2 + exactly one cycle_start event | Task 2 integration harness race test + Task 4 human verify step 5 |
| #4 Day-28 badge appears on /progress with no modal interruption | Task 4 human verify step 4 + Task 2 integration harness |
| #5 User at 23:00 Asia/Riyadh day 27 returns 06:00 next day to Day 28 | Task 1 unit test + Task 4 human verify step 6 |

Also verified:
- `npx tsc --noEmit && npm run build` pass.
- `bash scripts/guards/phase-07-anti-patterns.sh` exits 0.
- `node scripts/verify/phase-07-integration.mjs` exits 0.
- Unit test runs green.
</verification>

<success_criteria>
- Task 1 unit test passes the 6-vector calendarDay suite.
- Task 2 integration harness exits 0 on the happy path + race + idempotent + no-event-on-loser cases.
- Task 3 anti-pattern guard exits 0 (proving no banned patterns introduced).
- Task 4 human sign-off received.
- All 7 RETURN REQs and the applicable NFRs are structurally and observably satisfied.
</success_criteria>

<out_of_scope>
Banned anti-patterns — the Task 3 guard actively enforces all of these, but listed here for completeness:

- Confetti / fireworks / lottie on Day 28 or badge reveal.
- "Unlocked!" / "Achievement" / "Level up" English-loan vocabulary in source.
- Multi-step cycle-2 wizard.
- Auto-advance via `useEffect` on Day-28 save — CTA must be explicitly tapped.
- Streak continuation UI ("🔥 28 days streak!").
- "Top X% of users" comparison.
- Separate badge modal.
- Share button on the badge.
- Any `<Dialog>` / `<Modal>` / `<Drawer>` inside sacred components.
- Client-side PostHog emission on sacred paths.
- Session recording / heatmaps / autocapture.
- Modal interrupting reflection input on Day 28.
- New runtime dependency (NFR-08). Harness and guard use built-in Node + bash + existing grep.
- Retroactive badge backfill for existing users (Phase 8's job — do NOT run it here).
- Phase 8 badge variants (day_1/3/7/14/21/cycle_complete) — do NOT add them here.
- Any modification to sacred components (ReflectionJournal/AwarenessMeter/VerseBlock/HiddenLayer/BookQuote/SilenceGate) beyond what Plan 07.03 already did to DayExperience.
</out_of_scope>

<output>
After completion, create `.planning/phases/07-cycle-2-transition/07.06-SUMMARY.md`. Include:
- Table mapping each ROADMAP Success Criterion to the artifact that proved it.
- Test-run outputs (unit + harness + guard + build + tsc) — pasted verbatim.
- Human checkpoint transcript (what the reviewer observed + their sign-off signal).
- Final RETURN-01..07 status table (all seven marked as delivered).
- Any deferred follow-ups flagged by Plan 07.05's audit that should feed into the v1.3 backlog.

Then update `.planning/ROADMAP.md`: mark Phase 7 as `[x]`, record the commit hash of the final green-CI commit, and update the Progress Table row.
</output>
