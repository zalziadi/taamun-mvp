---
phase: 07-cycle-2-transition
plan: 03
type: execute
wave: 3
depends_on: [07.02]
files_modified:
  - src/components/DayExperience.tsx
autonomous: true
requirements:
  - RETURN-01
  - RETURN-03
  - RETURN-04
  - NFR-06
  - NFR-07
  - NFR-08
  - NFR-10

must_haves:
  truths:
    - "On Day 28 when the user has completed their reflection (`day === 28 && completed`), an inline CTA reading \"واصل الرحلة\" is rendered at the bottom of DayExperience — within the same view, no modal, no navigation away"
    - "Tapping the CTA POSTs to /api/program/start-cycle with { cycle: current_cycle + 1, expected_current_cycle: current_cycle }"
    - "On 2xx success, the client transitions to Day 1 of the new cycle (via router.refresh() + navigation)"
    - "On 409 (race), the CTA disables, shows a soft message referring the user to refresh, and does NOT POST again"
    - "The CTA button disables on first tap (prevents double-submit race on the SAME device)"
    - "No confetti, no fireworks, no \"Unlocked!\" vocabulary, no multi-step wizard, no auto-advance — user must explicitly tap"
    - "Celebration visual uses framer-motion only (already installed — NFR-08)"
    - "RTL correctness preserved (existing container is dir=rtl; copy is Arabic-native)"
  artifacts:
    - path: "src/components/DayExperience.tsx"
      provides: "Inline Day-28 CTA + click handler + framer-motion celebration"
      contains: "واصل الرحلة"
  key_links:
    - from: "src/components/DayExperience.tsx"
      to: "/api/program/start-cycle"
      via: "fetch POST on CTA click"
      pattern: "fetch\\(.*start-cycle"
    - from: "CTA click handler"
      to: "router.refresh() + navigate to day 1 of new cycle"
      via: "Next.js App Router navigation"
      pattern: "router\\.(refresh|push|replace)"
---

<objective>
Replace the silent Day-28 wall with an inline "واصل الرحلة" CTA rendered inside the same DayExperience view. Tapping it one-taps the user into cycle 2 by calling Plan 07.02's hardened `/api/program/start-cycle` route. No modal interrupts reflection input, no navigation away from the Day-28 screen until the user chooses, no automatic advance.

Purpose: Deliver ROADMAP Phase 7 Success Criteria #1 (CTA inline, no modal) and #2 (one-tap advance to Day 1 of cycle 2 — same 28 verses, Headspace model per RETURN-03). Client-side half of RETURN-01 / RETURN-03 / RETURN-04.
Output: `src/components/DayExperience.tsx` extended with a new inline section that renders only when `day === 28 && completed`, wired to POST start-cycle, with framer-motion celebration per RETURN-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cycle-2-transition/07-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/07-cycle-2-transition/07.02-PLAN.md
@CLAUDE.md

@src/components/DayExperience.tsx
@src/lib/taamun-content.ts
@src/lib/supabaseClient.ts

<interfaces>
<!-- Plan 07.02's route contract (already built when this plan executes) -->
<!--
POST /api/program/start-cycle
Body: { cycle: number, expected_current_cycle: number }
Responses:
  200 OK — { ok: true, cycle: number, archived: number, completed_cycles: number[] }
  409 Conflict — { error: "cycle_race", current_cycle: number }  // another device won
  500 Server Error — { error: "db_error", details: string }
  401 Unauthorized — { error: "unauthorized" }
-->

<!-- Existing DayExperience.tsx is a client component ("use client" on line 1) -->
<!-- It already imports PROGRESSION_MILESTONES from taamun-content.ts — day 28 is the last milestone -->
<!-- It already reads `day` from props and renders the full day flow (SilenceGate → VerseBlock → HiddenLayer → BookQuote → ReflectionJournal → AwarenessMeter → ShareCard → ProgressionBadge) -->
<!-- Existing supabase client import is @/lib/supabaseClient (browser) — used for completion checks -->

<!-- framer-motion is already installed (package.json) — NFR-08 preserved by using only what's installed -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inline Day-28 CTA with race-safe click handler</name>
  <files>src/components/DayExperience.tsx</files>
  <behavior>
    - Test A: When `day === 28` and the user has saved their Day-28 reflection (`completed === true`), the CTA "واصل الرحلة" is rendered at the end of the day view, below existing elements. NOT rendered for days 1-27.
    - Test B: Tapping the CTA once triggers a single POST to `/api/program/start-cycle` with the correct body. The button visually disables (aria-disabled, pointer-events-none, opacity reduction) immediately on first click — further taps on the same device produce zero additional POSTs.
    - Test C: On 2xx response, the client navigates such that the user lands on Day 1 of the new cycle (using `router.refresh()` to re-read server state, then the existing server-side redirect in `/program/page.tsx` sends them to the correct day-1 URL).
    - Test D: On 409 response, the CTA shows a soft message (Arabic: "تم بدء الحلقة من جهاز آخر. حدّث الصفحة لمتابعة رحلتك.") and stays disabled. No retry loop. No alarming error toast.
    - Test E: On 500 or network error, a recoverable message shows (Arabic: "تعذّر بدء الحلقة. جرّب مرة أخرى بعد لحظات."), the button re-enables so the user can retry manually.
    - Test F: framer-motion celebration — a subtle fade/scale animation plays on CTA appearance. No confetti. No fireworks. No emoji cascades.
  </behavior>
  <action>
Modify `src/components/DayExperience.tsx` in-place, following CLAUDE.md S-rules:
- S1: read the full file before editing.
- S2/S3: make the minimum diff. Do NOT refactor unrelated sections.
- S4: if the edit touches more than a single logical region, consider `git commit -m "chore: checkpoint"` first.
- S5: do not delete any imports unless verifiably unused.

Changes:

1. **Add imports** at the top (only if not already present):
   - `import { motion, AnimatePresence } from "framer-motion";` (verify framer-motion is listed in `package.json`; if the existing file already imports it elsewhere, reuse).
   - `import { useRouter } from "next/navigation";` (App Router hook — verify current Next.js 14 API via Context7 per CLAUDE.md rule 6 if unsure).

2. **Inside the main component**, after existing state declarations:
   - Add state: `const [ctaState, setCtaState] = useState<"idle" | "submitting" | "raced" | "error">("idle");`
   - Add router: `const router = useRouter();`
   - Add handler `handleStartCycle2` that:
     1. Guards against double-submit: `if (ctaState !== "idle") return;`
     2. Sets `setCtaState("submitting")`.
     3. Reads current cycle from the existing progress source the component already uses (the progress read on mount — reuse, do not add a new fetch). Let `currentCycle = progress?.current_cycle ?? 1`.
     4. POSTs to `/api/program/start-cycle`:
        ```ts
        const res = await fetch("/api/program/start-cycle", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            cycle: currentCycle + 1,
            expected_current_cycle: currentCycle,
          }),
        });
        ```
     5. Branch on `res.status`:
        - `200`: `router.refresh()` — this re-runs the server component for `/program` which redirects to day 1 of the new cycle per existing server-guard logic. Do NOT manually push a URL (server is authoritative).
        - `409`: `setCtaState("raced")` — leave button disabled, show the raced message.
        - other: `setCtaState("error")` after a brief delay, allow retry by resetting to `"idle"` after ~3s.
     6. Wrap the fetch in try/catch; on thrown error fall through the `other` branch.

3. **Render the CTA** as a new `<section>` appended at the END of the existing day-view JSX, visible only when `day === 28 && isCompleted`. **Canonical derivation (plan-checker gap #5):** `const isCompleted = progress.completed_days?.includes(28) ?? false;` — reuse whatever progress object is already in scope from existing code; do NOT add a redundant fetch or extra state variable.

   Structural requirements for the section:
   - Wrapped in `<AnimatePresence>` with one `<motion.section>` inside.
   - `initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, ease: "easeOut" }}` — subtle fade-in-up. NO spring that overshoots. NO particles. NO rotations.
   - RTL: container inherits the existing `dir="rtl"` from the parent; do NOT set `dir` locally.
   - Copy (Arabic, written — not translated):
     - Section heading: `الحلقة الثانية — نفس الآيات، تعمّق أعمق` (ties to RETURN-03: Headspace "same verses, deeper practice" model — NOT "new content").
     - Body sentence: `بعد هذه المرحلة، تعود الآيات كما هي، ويتغيّر قلبك. حين تكون مستعدًا، واصل.` (reflection-toned, not performative; NO "achievement unlocked" / "you did it!" vocabulary).
     - Button label: `واصل الرحلة` — exact text, no emoji.
     - Microcopy below button when idle: `(ستبقى تأمّلاتك السابقة محفوظة)` — trust-building, one line.
   - Button aria: `aria-busy={ctaState === "submitting"}`, `aria-disabled={ctaState !== "idle"}`, `disabled={ctaState !== "idle"}`.
   - Loading indicator: when `ctaState === "submitting"`, replace the label with `جارٍ البدء...` and a subtle opacity dim. Use existing Tailwind utility patterns from the file.
   - Raced state (`ctaState === "raced"`): render a calm message `تم بدء الحلقة من جهاز آخر. حدّث الصفحة لمتابعة رحلتك.` with a plain `<button onClick={() => router.refresh()}>تحديث</button>` — single refresh action, no auto-refresh timer.
   - Error state (`ctaState === "error"`): render `تعذّر بدء الحلقة. جرّب مرة أخرى بعد لحظات.` and leave the main button present but re-enabled after 3s.

4. **Do NOT** remove or modify the existing `ProgressionBadge` render on day 28. Plan 07.04 will adjust badge behavior; this plan ADDS the CTA alongside it.

5. **Do NOT** add any analytics call from this component — CLAUDE.md + CI grep guard prohibit `track()`/`posthog.capture()` inside `DayExperience.tsx` (ANALYTICS-10). The `cycle_start` event fires server-side from Plan 07.02's route. This is structural: no client emission is needed, wanted, or allowed here.

6. **Do NOT** introduce any new top-level dependency or new file. Everything stays within `DayExperience.tsx` + the existing framer-motion / next-navigation already in `package.json`.

DO NOT (anti-pattern sweep — aligned with 07-CONTEXT.md banned list):
- Confetti, fireworks, emoji cascades, particle showers.
- English-loan vocabulary: "Unlocked!", "Achievement", "Level up", "Completed!".
- Any `<Dialog>` / `<Modal>` / `<Drawer>` / blocking overlay.
- Auto-advance: do NOT POST on mount or on reflection save — only on explicit button tap.
- Multi-step wizard: one CTA → one POST → one navigation.
- Streak continuation UI ("🔥 28 days streak!").
- "You're in the top X% of users" comparison.
- Separate badge modal.
- Share button on the CTA or on the badge.
- Modifying reflection input, AwarenessMeter, or any sacred sub-component.
  </action>
  <verify>
    <automated>
# 1. The Arabic CTA text is present
grep -q "واصل الرحلة" src/components/DayExperience.tsx

# 2. Fetch to the correct endpoint
grep -q "/api/program/start-cycle" src/components/DayExperience.tsx

# 3. expected_current_cycle passed in the body (enforces Plan 07.02 contract)
grep -q "expected_current_cycle" src/components/DayExperience.tsx

# 4. framer-motion used, NOT confetti / lottie / any particle library
grep -q "framer-motion" src/components/DayExperience.tsx
! grep -iE "confetti|lottie|canvas-confetti" src/components/DayExperience.tsx

# 5. No banned English vocabulary
! grep -iE "unlocked|achievement|level up|streak" src/components/DayExperience.tsx

# 6. No client-side posthog emission (ANALYTICS-10 structural guard)
! grep -E "posthog\\.capture|track\\(" src/components/DayExperience.tsx

# 7. No modal / dialog component introduction
! grep -iE "<Dialog|<Modal|<Drawer" src/components/DayExperience.tsx

# 8. Compiles + builds
npx tsc --noEmit
npm run build

# 9. (If a test runner is available) run the DayExperience test file. If none exists, that's acceptable — Plan 07.06 adds the integration harness.
    </automated>
  </verify>
  <done>
- Inline CTA renders on Day 28 completed state; grep probes pass.
- Button disables after first tap (code path reviewed: setCtaState("submitting") before fetch).
- 409 response shows calm Arabic message + single manual refresh action.
- framer-motion animation is subtle (duration 0.6s, fade + 8px translate — no particles).
- Zero banned anti-patterns in the diff; grep enforces this.
- `npx tsc --noEmit && npm run build` pass.
  </done>
</task>

</tasks>

<verification>
Human-observable checks (Plan 07.06 will automate the flow; this lists the visible outcomes):

1. Open the app as a user whose `progress.current_day === 28 && progress.completed_days` includes 28 → the CTA is visible inline at the bottom of the Day-28 view. NO modal opens.
2. On days 1-27, the CTA is NOT rendered.
3. Tap the CTA once → within ~500ms the button disables, fetch fires, and (on success) the user lands on Day 1 of cycle 2. Lighthouse / DevTools Network shows exactly ONE POST to `/api/program/start-cycle`.
4. Simulate a 409 by firing a second POST in another tab first — the user's primary tab receives 409 and shows the refresh message. No error toast, no layout shift.
5. RTL spot-check on mobile viewport: copy flows right-to-left correctly; button label is legible; no English fallback visible.
6. Lighthouse a11y: no regression (should still be ≥95 per NFR-02).
</verification>

<success_criteria>
- DayExperience.tsx renders the inline CTA under the Day-28 completed condition (human + grep verified).
- CTA wired to Plan 07.02's route with `expected_current_cycle` sent — enforces RETURN-07 multi-device safety.
- framer-motion subtle animation only — RETURN-04 satisfied (no confetti).
- Arabic-native copy (RETURN-01 + NFR-07) — no machine translation, no English leak.
- Client-side analytics remain zero in DayExperience.tsx — ANALYTICS-10 preserved.
- No modal, no auto-advance, no multi-step wizard (07-CONTEXT.md invariants).
- `tsc --noEmit && build` pass.
</success_criteria>

<out_of_scope>
Banned anti-patterns — grep-enforced in &lt;verify&gt;:

- Confetti on Day 28 (framer-motion fade only).
- Fireworks / emoji cascades / particle shower.
- "Unlocked!" / "Achievement" / "Streak" English-loan vocabulary anywhere in this file.
- Multi-step cycle-2 wizard (one CTA → one POST → one nav).
- Auto-advance the moment Day 28 saves (user MUST tap).
- Streak continuation UI ("🔥 28 days streak!").
- "You're in the top X% of users" comparison.
- Separate badge modal (Plan 07.04 delivers badge silently).
- Share button on the CTA or on the Day-28 moment.
- Any `<Dialog>` / `<Modal>` / `<Drawer>` anywhere inside this component.
- Any client-side `posthog.capture(` / `track(` call (ANALYTICS-10).
- New runtime dependency (NFR-08).
- Changes to ReflectionJournal, AwarenessMeter, VerseBlock, HiddenLayer, BookQuote, SilenceGate — sacred components per ANALYTICS-10; only the CTA section is added.
- Session recording / heatmaps / autocapture hooks.
- Any English fallback visible to the user.
- Any blocking overlay that interrupts reflection input.
</out_of_scope>

<output>
After completion, create `.planning/phases/07-cycle-2-transition/07.03-SUMMARY.md`. Include: the exact final JSX for the CTA section, proof that grep probes pass, a short RTL screenshot description, and a note that RETURN-01, RETURN-03 (copy framing), and RETURN-04 (framer-motion no-confetti) are now delivered client-side.
</output>
