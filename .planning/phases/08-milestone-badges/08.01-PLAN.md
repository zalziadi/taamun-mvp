---
phase: 08-milestone-badges
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/badges/MilestoneBadge.tsx
autonomous: true
requirements:
  - BADGE-03
  - NFR-06
  - NFR-07
  - NFR-08
  - NFR-10

must_haves:
  truths:
    - "MilestoneBadge renders the 6 new variants: day_1, day_3, day_7, day_14, day_21, cycle_complete"
    - "Badge artwork is inline SVG — no external PNG, no Lottie, no canvas, no framer-motion inside this component"
    - "Arabic name + Eastern-Arabic numeral render as HTML sibling <span>s, never inside SVG <text> nodes (PITFALLS.md #8)"
    - "unlocked=false dims the badge via opacity-40 grayscale, same pattern as Phase 7 day_28"
  artifacts:
    - path: "src/components/badges/MilestoneBadge.tsx"
      provides: "BadgeCode union widened to include day_1 | day_3 | day_7 | day_14 | day_21 | day_28 | cycle_complete; BADGE_COPY lookup contains all 7 entries"
      contains: "day_1 | day_3 | day_7 | day_14 | day_21 | cycle_complete"
  key_links:
    - from: "src/components/badges/MilestoneBadge.tsx"
      to: "BadgeCode type export"
      via: "Phase 8 Plans 08.02/08.03/08.05 import this widened union"
      pattern: "export type BadgeCode"
---

<objective>
Widen the existing presentational MilestoneBadge component with 6 new variants (days 1/3/7/14/21 + cycle_complete) so that the server-side unlock triggers (Plan 08.02, 08.03) and the /progress grid (Plan 08.05) have a single SVG component to render every badge state in v1.2.

Purpose: Phase 7 shipped day_28 as proof-of-pattern. Phase 8 completes the 7-badge-per-cycle set locked in REQUIREMENTS.md decision #3 without changing the schema, the helper, or the analytics contract.

Output: `src/components/badges/MilestoneBadge.tsx` with a widened BadgeCode union and 7 BADGE_COPY entries. Zero new files. Zero schema change. Zero runtime dependency added (NFR-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-milestone-badges/08-CONTEXT.md
@CLAUDE.md
@src/components/badges/MilestoneBadge.tsx
@.planning/phases/07-cycle-2-transition/07.04-SUMMARY.md

<interfaces>
<!-- Existing Phase 7 shape of MilestoneBadge.tsx. Widen, don't rewrite. -->

Current (Phase 7):
```typescript
type BadgeCode = "day_28"; // single variant

export interface MilestoneBadgeProps {
  code: BadgeCode;
  unlocked: boolean;
  className?: string;
}

const BADGE_COPY: Record<BadgeCode, { name: string; numeral: string }> = {
  day_28: {
    name: "عتبة الثامن والعشرين",
    numeral: "٢٨",
  },
};
```

After Plan 08.01:
```typescript
export type BadgeCode =
  | "day_1"
  | "day_3"
  | "day_7"
  | "day_14"
  | "day_21"
  | "day_28"
  | "cycle_complete";

// BADGE_COPY: Record<BadgeCode, { name: string; numeral: string }>
// All 7 entries populated with classical-Arabic names + Eastern-Arabic numerals.
```

Palette (do not deviate — matches Phase 7 day_28):
- Gold stroke: `#C9A24B`
- Secondary (dim) stroke opacity: `0.7`
- Background: transparent (host page palette shows through)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen BadgeCode union + add 6 BADGE_COPY entries + 6 SVG variants</name>
  <files>src/components/badges/MilestoneBadge.tsx</files>
  <action>
Open `src/components/badges/MilestoneBadge.tsx` (read first — S1). Apply the following minimum-diff changes:

1. **Widen the BadgeCode union** and export it (downstream plans import this type):
   ```typescript
   export type BadgeCode =
     | "day_1"
     | "day_3"
     | "day_7"
     | "day_14"
     | "day_21"
     | "day_28"
     | "cycle_complete";
   ```
   (Previously `type BadgeCode = "day_28"` — now exported and widened to 7 variants.)

2. **Extend BADGE_COPY** with the 6 new entries. Use classical-Arabic register (no English-loan vocabulary — BADGE-04 tone). Recommended copy (planner's proposal; Ziad may adjust before merge):
   ```typescript
   const BADGE_COPY: Record<BadgeCode, { name: string; numeral: string }> = {
     day_1: { name: "عتبة البداية",            numeral: "١" },
     day_3: { name: "عتبة الثلاث",             numeral: "٣" },
     day_7: { name: "عتبة السبع",              numeral: "٧" },
     day_14: { name: "عتبة الأربعة عشر",       numeral: "١٤" },
     day_21: { name: "عتبة الواحد والعشرين",   numeral: "٢١" },
     day_28: { name: "عتبة الثامن والعشرين",   numeral: "٢٨" }, // Phase 7 — preserved
     cycle_complete: { name: "تمام الحلقة",     numeral: "◯" },
   };
   ```

3. **Render logic — switch the single SVG body into a per-variant renderer** that keeps the `day_28` geometry IDENTICAL to Phase 7 (concentric circles + 4 cardinal ticks) and gives each new variant a distinct but minimalist geometric treatment. Use a `renderSvgBody(code: BadgeCode)` internal function returning `<g>...</g>` children. Rules:
   - Stroke `#C9A24B` (gold) only. No fill. No gradient. No glow. No animation.
   - ViewBox `0 0 120 120`, width 96, height 96 (identical to Phase 7).
   - Each variant uses a different simple geometric motif to signal progression — suggestions (planner's discretion per 08-CONTEXT.md):
     * `day_1`  — single small centered circle (r=10) + one cardinal tick at top = "بداية"
     * `day_3`  — triangle inscribed in outer circle (r=54) + inner circle (r=14)
     * `day_7`  — 7-pointed radial ticks (rotated at 360/7 deg intervals) + outer circle
     * `day_14` — two concentric circles + 14 small dots around inner ring
     * `day_21` — 3 concentric circles + 7 radial ticks (3 × 7 = 21 visual cue)
     * `day_28` — PRESERVE Phase 7 geometry exactly (concentric circles r=54/42/10 + 4 cardinal ticks)
     * `cycle_complete` — outer solid ring (r=54, strokeWidth=2) enclosing the `day_28` inner geometry (concentric circles + cardinal ticks) = "closure around the full cycle"
   - All `<line>` and `<circle>` elements use `stroke="#C9A24B"`. No `strokeOpacity` below 0.6.
   - ZERO `<text>` elements anywhere in the SVG — Arabic name + numeral stay as HTML `<span>` siblings (PITFALLS.md #8 structural invariant).

4. **Preserve the wrapper exactly** as Phase 7 (the `<div dir="rtl">` with `inline-flex flex-col items-center gap-2` + dim classes when `unlocked=false`). Do NOT refactor the wrapper. Do NOT add hooks, state, effects, click handlers, fetch calls, or framer-motion (violates 08-CONTEXT banned anti-patterns).

5. **JSDoc header update**: update the top-of-file comment to note "Phase 8 widened this union to all 7 variants" and keep the PITFALLS.md #8 note intact.

**DO NOT**:
- Add `"use client"` (component is presentational; parent decides client/server boundary)
- Import framer-motion, lottie-react, canvas-confetti (NFR-08 + banned)
- Add share button, next/og import, OpenGraph export (BADGE-04 private-by-default)
- Add `useEffect` / `useState` / `fetch()` (layer discipline — CLAUDE.md: components are UI only)
- Add Arabic text inside any `<text>` SVG node (PITFALLS.md #8 — hard rule)
- Add rarity tier logic / progress bars / "Unlocked!" English-loan copy (banned anti-patterns)
- Touch any other file (S2 — single-file task)

Use the Phase 7 decision (per 07.04-SUMMARY.md): `unlocked: boolean` prop name is STRUCTURAL TypeScript and is not banned vocabulary. Keep the prop name.
  </action>
  <verify>
    <automated>
npx tsc --noEmit 2>&1 | grep -E "MilestoneBadge|badges/MilestoneBadge" ; \
grep -c "day_1\|day_3\|day_7\|day_14\|day_21\|day_28\|cycle_complete" src/components/badges/MilestoneBadge.tsx ; \
! grep -E "&lt;text[[:space:]&gt;]" src/components/badges/MilestoneBadge.tsx ; \
! grep -iE "framer-motion|lottie|canvas-confetti|next/og|opengraph|share" src/components/badges/MilestoneBadge.tsx ; \
! grep -E "useEffect|useState|fetch\(" src/components/badges/MilestoneBadge.tsx ; \
! grep -iE "Unlocked!|Achievement|Streak|Level up" src/components/badges/MilestoneBadge.tsx ; \
grep -q "export type BadgeCode" src/components/badges/MilestoneBadge.tsx ; \
npx tsc --noEmit &amp;&amp; npm run build
    </automated>
  </verify>
  <done>
- `BadgeCode` union exports all 7 variants (`export type BadgeCode`)
- `BADGE_COPY` record contains all 7 keys with classical-Arabic names + numerals
- SVG body branches per variant; zero `<text>` nodes anywhere
- `day_28` geometry byte-identical to Phase 7 (or visually equivalent — use git diff to confirm no regression on the Phase 7 variant)
- `npx tsc --noEmit && npm run build` pass clean
- No framer-motion / lottie / share / next/og / OpenGraph imports introduced (grep clean)
- No Arabic text inside SVG `<text>` nodes (grep clean)
- No "use client", no useState/useEffect/fetch (layer discipline preserved)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (no type errors on widened union)
- `npm run build` clean (Next.js still builds; component is tree-shakeable)
- `grep -c "day_1\|day_3\|day_7\|day_14\|day_21\|cycle_complete"` ≥ 6 matches in the component file
- `grep -E "<text[[:space:]>]"` returns no matches (PITFALLS.md #8 structural invariant preserved)
- `grep -iE "framer-motion|lottie|canvas-confetti|share|next/og"` returns no matches
- Visual spot-check in Storybook or a throwaway `/dev/badges` page (Plan 08.06 handles visual checkpoint) — no blocker for this plan
</verification>

<success_criteria>
- ROADMAP Phase 8 Criterion #5 (RTL correctness): satisfied structurally — Arabic glyphs render as HTML siblings, not inside SVG. Plan 08.05 will verify on device.
- BADGE-03 (inline SVG React components, no static PNG): satisfied — all 7 variants are inline SVG.
- NFR-06 (RTL correctness): satisfied — wrapper uses `dir="rtl"` identical to Phase 7.
- NFR-07 (Arabic-first copy): satisfied — all 6 new names in classical Arabic, never translated.
- NFR-08 (no new runtime deps): satisfied — zero imports added.
- NFR-10 (pre-merge tsc + build): enforced by verification block.
</success_criteria>

<risks_and_mitigations>
- **PITFALL #8 (RTL badge breakage — Arabic glyph disconnection inside SVG)**: Mitigated structurally. All 6 new variants continue the Phase 7 pattern of SVG-geometric-only + HTML-sibling Arabic. Grep probe `! grep -E "<text[[:space:]>]"` enforces.
- **Banned vocabulary drift** (08-CONTEXT.md "Banned anti-patterns"): Mitigated by grep probe `! grep -iE "Unlocked!|Achievement|Streak|Level up"`. Classical Arabic names proposed; Ziad can adjust pre-merge without breaking the grep.
- **Accidental layer violation** (component importing data-fetching or client hooks): Mitigated by grep probes `! grep -E "useEffect|useState|fetch\(" + ! grep -q "use client"`.
- **day_28 regression**: Mitigated by preserving Phase 7 geometry; Plan 08.06 integration test reads a Day-28 badge from storage and compares rendered DOM.
</risks_and_mitigations>

<validation_requirements>
Nyquist Dim 8 — automated verification:
- **Automated command exists**: `npx tsc --noEmit && npm run build` + 7 grep probes in the verify block.
- **< 60 seconds**: Yes — tsc + build is ~30s, grep probes are milliseconds.
- **Observable outcome**: All 7 BadgeCode variants render via a single component; HTML-sibling Arabic preserved; no banned imports.
</validation_requirements>

<out_of_scope>
Banned this plan (enforced by 08-CONTEXT.md):
- Streak counter UI ("🔥 7 days")
- Rare/legendary tier badges (no `rarity` prop, no `tier` prop)
- Badge progress bars ("3 of 6 — 50%!")
- Share buttons (no `<button>share</button>`, no `onShare` prop, no `next/og` import)
- Lottie / canvas-confetti animations
- framer-motion inside this component (Plan 08.05 adds a ≤600ms fade-in on the /progress grid, NOT here)
- Retrofitting Arabic inside SVG `<text>` nodes (PITFALLS.md #8 — hard rule)
- Adding state/effects/fetch to this component (layer discipline)
- Touching `src/lib/badges/unlock.ts` (Plan 08.02/08.03 own the helper widening)
- Touching `src/app/api/reflections/route.ts` (Plan 08.02 owns it)
- Touching `src/app/api/program/start-cycle/route.ts` (Plan 08.03 owns it)
- Creating a new file (this plan is a single-file widening)
</out_of_scope>

<output>
After completion, create `.planning/phases/08-milestone-badges/08.01-SUMMARY.md` documenting:
- Widened BadgeCode union shape
- Final chosen Arabic names for each new variant (Ziad may have adjusted)
- Final SVG geometry per variant (brief visual description)
- Grep verification results (8 probes)
- Git commit hash(es)
- `npx tsc --noEmit && npm run build` result
</output>
