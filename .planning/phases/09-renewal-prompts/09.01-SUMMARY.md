---
phase: 09-renewal-prompts
plan: 01
subsystem: database
tags: [supabase, postgres, migration, ddl, profiles, renewal, nfr-09]

# Dependency graph
requires:
  - phase: 07-cycle-2-transition
    provides: "v1.2 additive-migration pattern (ADD COLUMN IF NOT EXISTS + DO-block constraint guard) established in 20260419000000_v1_2_badges_and_cycle_guard.sql"
provides:
  - "profiles.original_gateway text column (nullable)"
  - "profiles_original_gateway_check CHECK constraint: NULL OR IN ('salla','tap','stripe','eid_code')"
  - "Stable schema contract that unblocks Plans 09.02 (backfill), 09.03 (webhook writes), 09.04 (RenewalBanner read), 09.05 (nudge orchestrator gateway routing)"
affects: [09.02-backfill, 09.03-webhook-writes, 09.04-renewal-banner, 09.05-nudge-orchestrator, post-phase-9-tightening-migration]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "NFR-09 two-step migration — step 1 (additive only, nullable, no DEFAULT) per Phase 7 precedent"
    - "NFR-10 idempotency via ADD COLUMN IF NOT EXISTS + pg_constraint existence check in DO block"
    - "CHECK-with-NULL permissiveness for grandfathered rows during additive window"

key-files:
  created:
    - "supabase/migrations/20260421000000_v1_2_profiles_original_gateway.sql"
  modified: []

key-decisions:
  - "CHECK constraint permits NULL so grandfathered rows remain valid until Plan 09.02 backfills them; NOT NULL tightening deferred to a future post-backfill migration outside Phase 9 scope."
  - "No DEFAULT on the column — keeps the ALTER TABLE metadata-only on PG 14+ (Supabase) and avoids a full-table rewrite. Plan 09.02 will populate values via UPDATE, which is cheaper to reason about and reverse."
  - "Values restricted to the four documented gateways ('salla','tap','stripe','eid_code') at the schema layer so webhook bugs in 09.03 cannot silently write garbage strings."
  - "File-timestamp 20260421000000 continues the ascending v1.2 chain (badges 04-19, badge-backfill 04-20, renewal column 04-21) so Supabase CLI applies them in deterministic order."

patterns-established:
  - "v1.2 additive-column pattern (mirrors Phase 7 migration 20260419 and Phase 8 migration 20260420): one ALTER + one guarded CHECK + COMMENT ON COLUMN documenting the owning phase + a commented-out DOWN block for operator reference."

requirements-completed: [RENEW-03]
# Note: RENEW-03 is partially addressed — this plan delivers the schema half
# (column + CHECK). The write-path half (webhook forward-fill) is Plan 09.03
# and the historical population is Plan 09.02. The requirements tracker marks
# RENEW-03 here because the plan frontmatter claims it; full closure happens
# when 09.02 + 09.03 ship and the verifier confirms gateway values populate
# for both new and existing rows.

# Metrics
duration: 4min
completed: 2026-04-19
---

# Phase 09 Plan 01: profiles.original_gateway Migration Summary

**Additive Postgres migration adding `profiles.original_gateway text NULL` with a CHECK constraint restricting values to `'salla' | 'tap' | 'stripe' | 'eid_code'` — NFR-09 step-1 contract that unblocks the Phase 9 renewal banner stack.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-19T11:53:00Z (approx)
- **Completed:** 2026-04-19T11:57:10Z
- **Tasks:** 1
- **Files modified:** 1 created, 0 modified

## Accomplishments
- Delivered the single SQL migration file declared in the plan frontmatter at the exact path and timestamp specified.
- Established the schema contract (`original_gateway text NULL` + CHECK with NULL-tolerance) that Plans 09.02, 09.03, 09.04, and 09.05 can code against without blocking on each other.
- Preserved all NFR-09 constraints: additive only, no DEFAULT, no NOT NULL, idempotent across re-runs.
- Documented a commented-out DOWN block for operator rollback reference without making the migration reversible in-place.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create additive migration for profiles.original_gateway** — `57f4008` (feat)

_Plan metadata commit will follow the state/roadmap update step._

## Files Created/Modified
- `supabase/migrations/20260421000000_v1_2_profiles_original_gateway.sql` — additive column + CHECK + COMMENT; 53 lines; zero runtime code impact.

## Decisions Made
- **CHECK permits NULL:** grandfathered rows (users who activated before this migration) must remain valid; enforcement will tighten only after Plan 09.02 audit.
- **No DEFAULT, no NOT NULL:** per NFR-09 step-1 contract and PG 14 metadata-only-rewrite best practice.
- **Comment rephrased to avoid literal `NOT NULL` string** so the plan's automated grep guard (`! grep -q "NOT NULL"`) passes while preserving the intent in natural language ("Column is nullable", "Nullability tightening is deferred").
- **No new runtime dependencies** (CLAUDE.md rule #6 respected — SQL-only change).

## Deviations from Plan

None — plan executed exactly as written. The only textual tweak (comment wording) was in service of the plan's own automated verification contract, not a structural deviation.

## Issues Encountered

- The plan's inline SQL used the phrase "No NOT NULL" in a comment, which its own `! grep -q "NOT NULL"` guard would reject. Resolved by rephrasing the comment to "Column is nullable" without changing any SQL semantics.
- Verification step could not shell out to bash (Bash denied for the aggregate grep chain); substituted dedicated Grep tool invocations covering the same assertions (present/absent matches for each required token). TypeScript and `npm run build` ran successfully.

## User Setup Required

None for this plan. Operator action required **outside** this plan:
- Apply `supabase/migrations/20260421000000_v1_2_profiles_original_gateway.sql` to staging, then production, as part of the next deploy. The migration is idempotent, so a retry is safe if the deploy pipeline double-runs.
- Post-apply sanity query on staging:
  ```sql
  SELECT column_name, is_nullable, column_default
  FROM information_schema.columns
  WHERE table_name = 'profiles' AND column_name = 'original_gateway';
  ```
  Expected: `is_nullable='YES'`, `column_default IS NULL`.

## Known Stubs

None. This plan ships schema only; no UI data paths are wired yet (by design — that is Plans 09.02–09.04).

## Next Phase Readiness
- Plans 09.02 (backfill), 09.03 (webhook writes), 09.04 (banner read), and 09.05 (nudge orchestrator gateway routing) can now code against `profiles.original_gateway` as a stable contract.
- Parallel execution: this plan was executed concurrently with 09.05 and 09.06 (no file overlap).
- RENEW-03 closure is pending — will fully flip to `[x]` when 09.02 + 09.03 ship and the Phase 9 verifier confirms values populate for both historical and new rows.

## Self-Check: PASSED

- FOUND: `supabase/migrations/20260421000000_v1_2_profiles_original_gateway.sql` (2478 bytes)
- FOUND: `.planning/phases/09-renewal-prompts/09.01-SUMMARY.md` (this file)
- FOUND commit: `57f4008` (feat(09-01): add profiles.original_gateway column)
- grep pass: `ADD COLUMN IF NOT EXISTS original_gateway text` present
- grep pass: `profiles_original_gateway_check` present
- grep pass: `NOT NULL` absent
- grep pass: `DEFAULT '` absent
- grep pass: no DROP statements outside the commented DOWN block
- `npx tsc --noEmit` clean
- `npm run build` clean (all 40+ routes compile)

---
*Phase: 09-renewal-prompts*
*Completed: 2026-04-19*
