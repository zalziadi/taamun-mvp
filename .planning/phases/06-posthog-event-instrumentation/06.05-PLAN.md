---
phase: 06-posthog-event-instrumentation
plan: 05
type: execute
wave: 3
depends_on: ["06.01", "06.02"]
# Wave bumped 2→3 (gsd-plan-checker 2026-04-19): 06.02 and 06.05 both modify
# src/lib/analytics/events.ts — serialize to avoid file-write race.
files_modified:
  - src/lib/analytics/events.ts
  - docs/analytics-event-catalog.md
autonomous: true
requirements: [ANALYTICS-04, ANALYTICS-05, ANALYTICS-06]
must_haves:
  truths:
    - "All 8 v1.2 events are documented with their intended call-site phase so Phases 7-11 executors know where to emit from"
    - "No call site for cycle_start / badge_unlock / renewal_prompted / referral_code_* / year_review_* exists in Phase 6 — only types + doc"
  artifacts:
    - path: "src/lib/analytics/events.ts"
      provides: "events.ts expanded with per-event JSDoc describing the future call site"
      contains: "@future-call-site"
    - path: "docs/analytics-event-catalog.md"
      provides: "Markdown catalog listing all 8 events, required props, owning phase, and emit-location"
      min_lines: 60
  key_links:
    - from: "docs/analytics-event-catalog.md"
      to: "src/lib/analytics/events.ts"
      via: "reference link; Arabic + English event descriptions"
      pattern: "events\\.ts"
---

<objective>
Finalize the type catalog with JSDoc + a human-readable event registry document. Phases 7-11 must know EXACTLY where to call `emitEvent()` for each of their 7 events without re-deriving the decision.

Purpose: ANALYTICS-04, -05, -06 require that the type stubs for `cycle_start`, `badge_unlock`, `renewal_prompted` exist now so their future executors don't accidentally invent a new event shape. Plan 06.01 created the union; this plan adds JSDoc + a catalog doc that serves as the cross-phase contract.

Output:
- `src/lib/analytics/events.ts` — every variant of `TypedEvent` gets a JSDoc block with `@owning-phase`, `@future-call-site`, `@req-id`, Arabic-friendly one-line description
- `docs/analytics-event-catalog.md` — human-readable reference (Arabic-first per NFR-07; English synonyms permitted for technical terms)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-posthog-event-instrumentation/06-CONTEXT.md
@.planning/phases/06-posthog-event-instrumentation/06.01-PLAN.md
@.planning/ROADMAP.md
@src/lib/analytics/events.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add per-variant JSDoc to TypedEvent in events.ts</name>
  <files>src/lib/analytics/events.ts</files>
  <behavior>
    - Each of the 8 union variants gets a JSDoc block above it
    - Each JSDoc includes: `@event-name`, `@req-id`, `@owning-phase`, `@future-call-site`, one-line Arabic description, one-line English description
    - No change to the type shape itself — only documentation
  </behavior>
  <action>
    For each variant, add a JSDoc block. Example for `day_complete`:

    ```typescript
    /**
     * إكمال يوم — يُطلق بعد حفظ ذكر/تأمل اليوم بنجاح.
     * Fires after a successful day-reflection save.
     * @event-name day_complete
     * @req-id ANALYTICS-03
     * @owning-phase Phase 6 (proof-of-pipeline)
     * @future-call-site src/app/api/program/progress/route.ts POST success
     */
    ```

    Repeat for all 8 variants. The owning-phase mapping:
    - `day_complete` → Phase 6 (Plan 06.04) — ALREADY wired
    - `cycle_start` → Phase 7 — call site: `src/app/api/program/start-cycle/route.ts` POST success
    - `badge_unlock` → Phase 8 — call site: `src/app/api/badges/unlock/route.ts` POST success (doesn't exist yet)
    - `renewal_prompted` → Phase 9 — call site: `RenewalBanner.tsx` first-render (client-side exception, but still typed)
    - `referral_code_generated` → Phase 10 — call site: `src/app/api/referral/create/route.ts`
    - `referral_code_redeemed` → Phase 10 — call site: `src/app/api/activate/route.ts` on FRIEND-* success
    - `year_review_opened` → Phase 11 — call site: `src/app/year-in-review/page.tsx` server component load
    - `year_review_shared` → Phase 11 — call site: `src/app/api/year-in-review/share/route.ts` POST

    NFR-07 (Arabic-first): each description must have an Arabic line FIRST, English line SECOND. The Arabic must be written natively (not machine-translated). Sample Arabic one-liners:
    - `day_complete`: "إكمال يوم — يُطلق بعد حفظ ذكر/تأمل اليوم بنجاح."
    - `cycle_start`: "بدء حلقة جديدة — يُطلق عند الانتقال إلى الحلقة التالية بعد اليوم ٢٨."
    - `badge_unlock`: "منح شارة — يُطلق عند تجاوز عتبة (يوم ١، ٣، ٧، ١٤، ٢١، ٢٨)."
    - `renewal_prompted`: "عرض لافتة التجديد — يُطلق عند أول ظهور للافتة في جلسة."
    - `referral_code_generated`: "توليد رمز دعوة — يُطلق عند إنشاء رمز FRIEND-*."
    - `referral_code_redeemed`: "استخدام رمز دعوة — يُطلق عند تفعيل رمز FRIEND-*."
    - `year_review_opened`: "فتح الحصيلة السنوية — يُطلق عند تحميل /year-in-review."
    - `year_review_shared`: "مشاركة الحصيلة السنوية — يُطلق عند تصدير بطاقة المشاركة."
  </action>
  <verify>
    <automated>grep -c "@event-name" src/lib/analytics/events.ts</automated>
  </verify>
  <done>
    - `grep -c "@event-name" src/lib/analytics/events.ts` returns 8
    - `grep -c "@owning-phase" src/lib/analytics/events.ts` returns 8
    - `grep -c "@future-call-site" src/lib/analytics/events.ts` returns 8
    - `npx tsc --noEmit` still passes (JSDoc doesn't break types)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create docs/analytics-event-catalog.md</name>
  <files>docs/analytics-event-catalog.md</files>
  <behavior>
    - Arabic-first markdown document (RTL) with a table of all 8 events
    - Columns: اسم الحدث (event name) · REQ · المرحلة المالكة · موقع الإطلاق · الخصائص (properties)
    - Footer notes: (1) all emissions go through `emitEvent()` from `src/lib/analytics/server.ts`; (2) property whitelist enforced by `assertAllowedProperties` + CI grep; (3) `person_profiles: "never"` preserved; (4) no client-side emission except `$pageview` from `PageviewTracker.tsx`
  </behavior>
  <action>
    Create `docs/analytics-event-catalog.md`. Structure:

    ```markdown
    # سجل أحداث تحليلات PostHog — v1.2

    **المرجع:** REQUIREMENTS.md §ANALYTICS · 06-CONTEXT.md · src/lib/analytics/events.ts

    > الأحداث الثمانية أدناه مُعرَّفة كـ `TypedEvent` في `src/lib/analytics/events.ts`.
    > لا تُضاف أحداث جديدة بدون تحديث هذا السجل + الاتحاد المُميَّز + اختبارات الأنماط المحظورة.

    ## الأحداث

    | اسم الحدث | REQ | المرحلة | موقع الإطلاق | الخصائص |
    |---|---|---|---|---|
    | `day_complete` | ANALYTICS-03 | 6 | `src/app/api/program/progress/route.ts` POST | `day_number, cycle_number, tier` |
    | `cycle_start` | ANALYTICS-04 | 7 | `src/app/api/program/start-cycle/route.ts` POST | `new_cycle_number, prior_cycle_days_completed, tier` |
    | `badge_unlock` | ANALYTICS-05 | 8 | `src/app/api/badges/unlock/route.ts` POST | `badge_code, day_number, cycle_number` |
    | `renewal_prompted` | ANALYTICS-06 | 9 | `src/components/RenewalBanner.tsx` (first render) | `renewal_days_remaining, gateway, tier` |
    | `referral_code_generated` | ANALYTICS-07 | 10 | `src/app/api/referral/create/route.ts` | `referral_code_prefix` |
    | `referral_code_redeemed` | ANALYTICS-07 | 10 | `src/app/api/activate/route.ts` (FRIEND-*) | `referral_code_prefix` |
    | `year_review_opened` | ANALYTICS-08 | 11 | `src/app/year-in-review/page.tsx` server load | `year_key, reflections_count` |
    | `year_review_shared` | ANALYTICS-08 | 11 | `src/app/api/year-in-review/share/route.ts` | `year_key, reflections_count` |

    ## قواعد غير قابلة للتفاوض

    1. كل إطلاق يمر عبر `emitEvent()` من `src/lib/analytics/server.ts` (لا `posthog-node`، لا SDK)
    2. `person_profiles: "never"` محفوظ في `initAnalytics()` — ANALYTICS-11
    3. `capture_pageview: false` محفوظ — `PageviewTracker.tsx` يتولى ذلك
    4. القائمة البيضاء للخصائص مفروضة وقت التشغيل (`assertAllowedProperties`) ووقت البناء (CI grep — Plan 06.06)
    5. أنماط ممنوعة: `*_email`, `*_phone`, `reflection_*`, `verse_*`, `journal_*`, `message_*`, `prayer_*`
    6. صفر `track()` / `posthog.capture(` داخل `src/app/day/**`, `src/app/reflection/**`, `src/app/book/**`, `src/app/program/day/**`, `/api/guide/**`
    7. صفر `track()` داخل `DayExperience.tsx`, `ReflectionJournal.tsx`, `AwarenessMeter.tsx`, `BookQuote.tsx`, `VerseBlock.tsx`, `HiddenLayer.tsx`, `SilenceGate.tsx`

    ## كيف يضيف المستقبل حدثًا جديدًا (checklist)

    - [ ] أضِف متغيرًا جديدًا إلى `TypedEvent` في `src/lib/analytics/events.ts`
    - [ ] تأكّد أن كل مفتاح في `properties` يظهر في `ALLOWED_PROPERTY_KEYS`
    - [ ] أضف سطرًا إلى الجدول أعلاه
    - [ ] أضف اختبار في `events.test.ts` يتأكّد أن الخصائص لا تطابق أي نمط محظور
    - [ ] لا تُطلق الحدث من داخل مكوّن أو مسار في القائمة المقدسة

    ## مهام المؤسس اليدوية (PostHog Dashboard) — Pitfall #28

    > هذه الإعدادات خارج الكود — تُضبط مرة واحدة في لوحة PostHog.

    - [ ] **تنبيه الحصة (Billing → Usage Alerts):** تنبيه عند 70% + تنبيه عند 90% من الحصة الشهرية المجانية (1M حدث/شهر). الحساب المتوقع: 1,500 مشترك × ~10 أحداث/يوم × 30 = 450K/شهر — تحت السقف لكن مع هامش ضيّق.
    - [ ] **إيقاف Session Recording وHeatmaps** على مستوى المشروع (Project Settings → Recordings/Toolbar) — حتى لو أحد الإعدادات في الكود انهار، الطبقة الثانية من الحماية موجودة.
    - [ ] **قفل Autocapture** على المشروع (Project Settings → Autocapture: OFF) — نحن نُطلق صراحةً، لا تلقائيًا.
    - [ ] **تعيين Retention للأحداث الحسّاسة** إلى 30 يومًا (Data Management → Event Definitions) — ما عدا `day_complete`/`cycle_start` (نحتفظ بها لتحليل الـ funnels).

    تاريخ آخر مراجعة لمهام المؤسس: `[اكتبه عند أول ضبط]`
    ```

    Also include a brief English summary at the bottom for non-Arabic-reading contributors:

    ```markdown
    ---
    ## English summary (for contributors)

    8 typed events, all emitted server-side via `emitEvent()` (except `$pageview` from `PageviewTracker.tsx`).
    Property whitelist is enforced at runtime and build time. Sacred paths/components never call `track()`.
    See `src/lib/analytics/events.ts` for the discriminated union and `06-CONTEXT.md` for the full policy.
    ```
  </action>
  <verify>
    <automated>test -f docs/analytics-event-catalog.md &amp;&amp; grep -c "^|" docs/analytics-event-catalog.md</automated>
  </verify>
  <done>
    - `docs/analytics-event-catalog.md` exists
    - Contains a table with all 8 events (grep count of `|` lines ≥ 10 including header + separator + 8 rows)
    - Footer rules section lists the 7 banned patterns
  </done>
</task>

</tasks>

<verification>
1. `grep -c "@event-name" src/lib/analytics/events.ts` returns 8
2. `docs/analytics-event-catalog.md` exists with table of 8 events
3. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- All 8 events documented in-code (JSDoc) AND in a human-readable catalog
- Phases 7-11 executors can copy the call-site path from the catalog without re-deriving
- ANALYTICS-04 / -05 / -06 / -07 / -08 type stubs all documented with owning phase
</success_criteria>

<out_of_scope>
- Actually wiring cycle_start / badge_unlock / renewal_prompted / referral_code_* / year_review_* (those are Phase 7-11 work)
- PostHog session recording / heatmaps
- Feature flags on sacred routes
- `posthog-node`
- Segment / Mixpanel / GA4
- CI grep lint rule (Plan 06.06)
</out_of_scope>

<output>
Create `.planning/phases/06-posthog-event-instrumentation/06-05-SUMMARY.md` with the catalog structure + per-event owning-phase list.
</output>
