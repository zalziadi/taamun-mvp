# Requirements — v2.2 (archived)

Snapshot of v2.2 requirements as shipped 2026-04-20.

---

## Mission

Turn the v2.1 snapshot dashboard into a trend dashboard. Daily capture
+ 30-day sparklines + CSV export.

---

## Must-have ✅

- ✅ `ops_snapshots` migration
- ✅ Daily cron `/api/cron/ops-snapshot` at 23:55 UTC
- ✅ 30-day sparkline per metric on `/admin/ops`
- ✅ CSV export at `/api/ops/export`

---

## Out of scope

- Time-range filter on export
- Metric alerts / thresholds
- Websocket streaming

---

## Open questions — resolved

1. **Retention:** ✅ keep all snapshots (tiny table)
2. **Snapshot time:** ✅ 23:55 UTC
3. **Sparkline library:** ✅ zero deps, inline SVG
4. **CSV scope:** ✅ full table
