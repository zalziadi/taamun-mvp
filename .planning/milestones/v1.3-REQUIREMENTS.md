# Requirements — v1.3 (Archived)

All requirements from v1.3 with final status.

---

## Must-have requirements

- [x] PWA install prompt (opt-in) shown after day 7, once per session, dismissable
  - ✅ Shipped with day-28 re-prompt logic + iOS fallback tip
- [x] Offline reading for today's verse (service worker caches last 3 days)
  - ✅ Service worker extended with network-first HTML caching + static cache-first
- [x] Invite link system (`/invite/[code]`) with referral tracking
  - ✅ Full DB + API + landing page + account widget
- [x] "Year in Taamun" recap page + email (for users with ≥ 90 days history)
  - ✅ `/recap` page shipped; optional year-end batch email deferred to v1.4 operations

## Should-have

- [x] Shareable themes/insights (opt-in, anonymous, static pages)
  - ✅ `/shared/[slug]` + moderation + OG metadata
- [x] Voice tasbeeh (hands-free dhikr counter)
  - ✅ Web Speech API + 3 phrases + manual fallback
- [ ] Referral reward: 1 free month for both parties on successful subscription
  - **Partial:** reward infrastructure + tracking shipped. Actual credit application (adding 30 days to `expires_at`) deferred to v1.4 post-conversion webhook

## Nice-to-have

- [ ] OG image generation for shareable quotes (Arabic calligraphy aesthetic)
  - ❌ Deferred (uses plain text OG meta for now)
- [ ] Leaderboard-free progress sharing ("I finished day 28 of تمعّن")
  - ❌ Deferred — covered partially by shareable insights
- [ ] Mobile native wrapper (Capacitor) if PWA gaps appear on iOS
  - ❌ Deferred — PWA should suffice for Android + iOS Safari

---

## Non-functional requirements

- [x] Performance: LCP < 6s on 3G mobile — **maintained** (no regressions)
- [x] Accessibility: Lighthouse A11y ≥ 0.95 — **gated in CI**
- [x] SEO: `/shared/*` pages fully indexable + structured data
  - ✅ `generateMetadata` with OG tags per insight
- [x] Cost: v1.3 feature work < 3000 SAR total — **$0 operational increase**
- [x] Privacy: sharing is opt-in only; no PII in shared pages — **enforced**
- [x] Content moderation: short content reviewable by founder
  - ✅ Auto-flag for URLs, instant for clean short content

---

## Decisions resolved (2026-04-18)

User answered "yes all" on 5 open questions:

1. **Invite reward type:** ✅ Free month for both parties
2. **Recap trigger:** ✅ Rolling per-user day 365 + optional calendar year-end batch
3. **PWA install prompt:** ✅ Day 7, re-prompt day 28 if dismissed
4. **Share moderation:** ✅ Instant for ≤200 chars no links, flag otherwise
5. **Voice tasbeeh tech:** ✅ Web Speech API first, Munsit fallback

---

## Deferred to v1.4

- Actual reward credit application (30-day `expires_at` bump on successful subscription)
- OG image generation (Arabic calligraphy cards)
- Calendar year-end recap email batch
- In-app community (threaded discussions)
- Creator mode (users publish their own 28-day journeys)
- Moderation UI for `status='flagged'` insights

---

## Validation

Infrastructure validated technically (tsc ✅ build ✅ deploy ✅).
End-user validation pending usage after migration activation.

Success criteria (to measure post-activation):
- PWA install rate ≥ 10% of active users ≥ day 30
- Invite → signup conversion ≥ 5%
- ≥ 20 shared insight pages with ≥ 5 views each
- Year-in-Taamun recap opens ≥ 40% of eligible users
- Zero content moderation incidents in shared pages
